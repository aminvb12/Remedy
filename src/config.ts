// Update once the GitHub App is created — the slug comes straight from its
// install URL, e.g. github.com/apps/<slug>. Nothing else in the frontend
// needs to change when this does.
export const GITHUB_APP_SLUG = "remedy-incident-resolver";

export const GITHUB_APP_INSTALL_URL = `https://github.com/apps/${GITHUB_APP_SLUG}/installations/new`;
